project Test_Statistics is
    for Source_Dirs use ("src");
    for Object_Dir use "obj";
    for Main use ("test_statistics.adb");

   package Compiler is
     for Default_Switches ("Ada") use ("-g", "-gnat2022");
   end Compiler;

   package Linker is
      for Linker_Options use ("-g", "-v", "-lgpiod");
   end Linker;
end Test_Statistics;
